<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/coffee_ic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/coffee_ic_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeteriaManagement"
        tools:targetApi="31">
        <activity
            android:name=".UI.activities.StaffDashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".UI.activities.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".UI.activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".UI.activities.GetStartedActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>